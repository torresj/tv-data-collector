apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tv-spain-schedule-collector-pvc
spec:
  storageClassName: nfs-storageclass
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
